# =================================================================
# Configurações de Produção para Redpanda na Railway
# Copie este arquivo para .env e configure as variáveis
# =================================================================

# =================================================================
# CONFIGURAÇÕES DE AUTENTICAÇÃO (OBRIGATÓRIAS)
# =================================================================

# Usuário administrador do Redpanda
REDPANDA_ADMIN_USER=admin
REDPANDA_ADMIN_PASSWORD=admin123

# Usuários para clientes (opcional - serão criados automaticamente)
PRODUCER_USER=producer
PRODUCER_PASSWORD=producer123
CONSUMER_USER=consumer
CONSUMER_PASSWORD=consumer123

# =================================================================
# CONFIGURAÇÕES DE REDE
# =================================================================

# Portas expostas (Railway irá configurar automaticamente)
REDPANDA_KAFKA_PORT=9092
REDPANDA_SCHEMA_REGISTRY_PORT=8081
REDPANDA_PANDAPROXY_PORT=8082
REDPANDA_METRICS_PORT=9644
CONSOLE_PORT=8080

# Endereços de anúncio (configure com o domínio da Railway)
REDPANDA_ADVERTISE_RPC_SERVER=redpanda:33145
REDPANDA_ADVERTISE_KAFKA_API=redpanda:9092
REDPANDA_ADVERTISE_PANDAPROXY_API=redpanda:8082
REDPANDA_ADVERTISE_SCHEMA_REGISTRY_API=redpanda:8081

